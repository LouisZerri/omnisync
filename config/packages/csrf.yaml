# CSRF stateless pour le login/logout (champs gérés en JS) ;
# les formulaires applicatifs utilisent le CSRF stateful (basé session), plus robuste.
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            - authenticate
            - logout
