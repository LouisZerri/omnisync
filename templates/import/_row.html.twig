{% set style = {
    'pending': {'badge': 'bg-gray-100 text-gray-600', 'dot': 'bg-gray-400'},
    'running': {'badge': 'bg-amber-50 text-amber-700', 'dot': 'bg-amber-500'},
    'done':    {'badge': 'bg-emerald-50 text-emerald-700', 'dot': 'bg-emerald-500'},
    'failed':  {'badge': 'bg-red-50 text-red-700', 'dot': 'bg-red-500'},
}[import.status.value] %}
<tr id="import-{{ import.id }}" class="transition hover:bg-gray-50">
    <td class="px-6 py-4 font-medium text-gray-900">{{ import.originalFilename }}</td>
    <td class="px-6 py-4">
        <span class="inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-medium {{ style.badge }}">
            <span class="h-1.5 w-1.5 rounded-full {{ style.dot }}"></span>{{ import.status.label }}
        </span>
    </td>
    <td class="px-6 py-4 text-right text-gray-900">{{ import.createdCount }}</td>
    <td class="px-6 py-4 text-right text-gray-900">{{ import.updatedCount }}</td>
    <td class="px-6 py-4 text-right">
        {% if import.errorCount > 0 %}
            <a href="{{ path('app_import_show', {id: import.id}) }}" class="font-medium text-red-600 hover:underline">{{ import.errorCount }}</a>
        {% else %}
            <span class="text-gray-400">0</span>
        {% endif %}
    </td>
    <td class="px-6 py-4 whitespace-nowrap font-mono text-xs text-gray-500">{{ import.createdAt|date('d/m/Y H:i:s') }}</td>
</tr>
