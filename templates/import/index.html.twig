{% extends 'layout/app.html.twig' %}

{% block title %}Imports · OmniSync{% endblock %}
{% block page_title %}Imports{% endblock %}

{% block app_content %}
    <div class="mb-6 max-w-2xl rounded-xl border border-gray-200 bg-white p-6">
        <div class="mb-4 rounded-lg bg-gray-50 p-4 text-sm text-gray-600">
            <p class="mb-1 font-medium text-gray-900">Format attendu</p>
            <p>Un fichier CSV avec une ligne d'en-tête et les colonnes : <span class="font-mono text-xs">sku, name, description, price, stock</span>.</p>
            <p class="mt-1">Le prix est en euros (ex. <span class="font-mono text-xs">199.99</span>). Si une référence existe déjà, le produit est mis à jour.</p>
        </div>

        {{ form_start(form) }}
        <div data-controller="file-input">
            <label class="flex cursor-pointer flex-col items-center justify-center gap-2 rounded-lg border-2 border-dashed border-gray-300 bg-gray-50 px-6 py-8 text-center transition hover:border-brand hover:bg-brand/5">
                <svg class="h-8 w-8 text-gray-400" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M3 16.5v2.25A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75V16.5m-13.5-9L12 3m0 0l4.5 4.5M12 3v13.5" /></svg>
                <span class="text-sm text-gray-600"><span class="font-semibold text-brand">Cliquez pour choisir</span> un fichier CSV</span>
                <span data-file-input-target="name" class="text-xs text-gray-400">Aucun fichier sélectionné</span>
                {{ form_widget(form.file, {'attr': {'class': 'sr-only', 'data-file-input-target': 'input', 'data-action': 'change->file-input#display'}}) }}
            </label>
            <div class="mt-1 text-sm text-red-600">{{ form_errors(form.file) }}</div>
        </div>
        <div class="mt-6">
            <button type="submit" class="cursor-pointer rounded-lg bg-brand px-4 py-2 text-sm font-medium text-white transition hover:opacity-90">
                Lancer l'import
            </button>
        </div>
        {{ form_end(form) }}
    </div>

    <div class="overflow-hidden rounded-xl border border-gray-200 bg-white">
        <table class="w-full text-sm">
            <thead>
            <tr class="border-b border-gray-200 text-left text-xs font-medium uppercase tracking-wide text-gray-500">
                <th class="px-6 py-3">Fichier</th>
                <th class="px-6 py-3">Statut</th>
                <th class="px-6 py-3 text-right">Créés</th>
                <th class="px-6 py-3 text-right">Mis à jour</th>
                <th class="px-6 py-3 text-right">Erreurs</th>
                <th class="px-6 py-3">Lancé le</th>
            </tr>
            </thead>
            <tbody class="divide-y divide-gray-100">
            {% for import in pagination %}
                {% set style = {
                    'pending': {'badge': 'bg-gray-100 text-gray-600', 'dot': 'bg-gray-400'},
                    'running': {'badge': 'bg-amber-50 text-amber-700', 'dot': 'bg-amber-500'},
                    'done':    {'badge': 'bg-emerald-50 text-emerald-700', 'dot': 'bg-emerald-500'},
                    'failed':  {'badge': 'bg-red-50 text-red-700', 'dot': 'bg-red-500'},
                }[import.status.value] %}
                <tr class="transition hover:bg-gray-50">
                    <td class="px-6 py-4 font-medium text-gray-900">{{ import.originalFilename }}</td>
                    <td class="px-6 py-4">
                        <span class="inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-medium {{ style.badge }}">
                            <span class="h-1.5 w-1.5 rounded-full {{ style.dot }}"></span>{{ import.status.label }}
                        </span>
                    </td>
                    <td class="px-6 py-4 text-right text-gray-900">{{ import.createdCount }}</td>
                    <td class="px-6 py-4 text-right text-gray-900">{{ import.updatedCount }}</td>
                    <td class="px-6 py-4 text-right">
                        {% if import.errorCount > 0 %}
                            <a href="{{ path('app_import_show', {id: import.id}) }}" class="font-medium text-red-600 hover:underline">{{ import.errorCount }}</a>
                        {% else %}
                            <span class="text-gray-400">0</span>
                        {% endif %}
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap font-mono text-xs text-gray-500">{{ import.createdAt|date('d/m/Y H:i:s') }}</td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="6" class="px-6 py-12 text-center text-sm text-gray-500">Aucun import pour le moment.</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    {% if pagination.getPageCount > 1 %}
        <div class="mt-6 flex justify-center">
            {{ knp_pagination_render(pagination) }}
        </div>
    {% endif %}
{% endblock %}
